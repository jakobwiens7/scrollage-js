{
  "name": "scrollagejs",
  "version": "0.8.8",
  "description": "Animate elements easily by scrolling position.",
  "author": "Jakob Wiens",
  "license": "MIT",
  "keywords": [
    "scrolling",
    "animation",
    "parallax"
  ],
  "main": "dist/scrollage.min.js",
  "scripts": {
    "build": "webpack",
    "clean": "rm -rf node_modules && rm dist/main.js",
    "reinstall": "npm run clean && npm install",
    "rebuild": "npm run clean && npm install && npm run build"
  },
  "devDependencies": {
    "@babel/preset-env": "latest",
    "webpack": "^5.97.1",
    "webpack-cli": "^6.0.1"
  }
}